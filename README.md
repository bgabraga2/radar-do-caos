# Radar do Caos

Dashboard Streamlit do desafio Locaweb (FIAP) — previsão de incidentes, tendências, impacto em OLA e apoio à decisão operacional.

## Rodar local

```bash
pip install -r requirements.txt
streamlit run app/Home.py
```
